// src/utils/validate.js
// Example usage: you can implement actual validation using Joi or express-validator

exports.validateRequest = (schema, data) => {
    // placeholder for schema validation
    // e.g. const { error } = schema.validate(data);
    // if (error) throw new Error(error.details[0].message);
    return true;
};
